# macOS Finder artifacts — matched at ANY depth (a bare pattern with no slash
# applies in every directory, so one entry covers the whole repo)
.DS_Store
._*
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes
.fseventsd
.DocumentRevisions-V100
.TemporaryItems
.VolumeIcon.icns

# tooling
.serena/
custom/tools/dcp_decrypt
